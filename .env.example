# Model Provider Configuration
# Choose your model provider (bedrock, anthropic, openai, etc.)
MODEL_PROVIDER=bedrock
MODEL_ID=us.amazon.nova-pro-v1:0
MODEL_TEMPERATURE=0.3

# AWS Configuration (if using Bedrock)
AWS_REGION=us-east-1
AWS_PROFILE=default

# Anthropic API Key (if using Anthropic)
# ANTHROPIC_API_KEY=your_api_key_here

# OpenAI API Key (if using OpenAI)
# OPENAI_API_KEY=your_api_key_here

# MCP Server Configuration
# PingOne MCP Server settings
PINGONE_MCP_COMMAND=uvx
PINGONE_MCP_ARGS=pingone-mcp-server

# Microsoft Graph MCP Server settings
MSGRAPH_MCP_COMMAND=uvx
MSGRAPH_MCP_ARGS=msgraph-mcp-server
